streamlit>=1.28.0
# pywin32 is Windows-only and required for Outlook automation on the desktop.
# It is intentionally excluded here so Streamlit Cloud (Linux) can build the app.
# Desktop users should install it separately: pip install pywin32
